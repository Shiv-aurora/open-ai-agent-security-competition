# Submission history

Unpacked the Kaggle JED SDK (`aicomp_sdk` + evaluation gateway) so local tests
can import the same contracts, predicates, and OptimalGuardrail the hosted
rerun uses. No attack is submitted from this snapshot.
